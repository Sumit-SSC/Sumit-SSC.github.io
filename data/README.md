# Data

Structured content for the site.

## Files
- `projects.json` — single source of truth for project cards and detail pages.

## Usage
Update `projects.json` and refresh the site to reflect changes.
